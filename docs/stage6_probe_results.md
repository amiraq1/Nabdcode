# Stage 6 — Verifier Gate Live Evidence (Termux, HEAD = 4b5cc7d)

Generated by `scripts/probe_stage6_gate.py` against the REAL `ExecutionLoop`
path (the same path `main.py` CLI activates). The probe monkeypatches
`llm_router.run_verifier_check` + `verifier_router.generate_response` ONLY to
capture the prompt and call count — no production code is modified.

## Environment
```
$ git rev-parse --short HEAD
4b5cc7d
$ python3 -V
Python 3.11.x (Termux / android-arm64)
```

## A3 — supported multi-file task (accept path)
```
[MODE] A3
[SIGNAL] TERMINATE
[REAL_READS] 3
[VERIFIER_CALLS] 1
[VERIFIER_CRITIQUE_EVENTS] 0
[TEMPERATURE_FORCED] [0]
[CONTEXT_LEAK_SUSPECTED] False
--- evidence_summary ---
- [file_system] content of core/_env.py: API_KEY handling here
- [file_system] content of llm_router.py: API_KEY handling here
- [file_system] content of core/config.py: API_KEY handling here
```
Verdict: PASS accepted → clean TERMINATE. Verifier prompt contains ONLY
{goal, final_answer, evidence_summary}; the maker's chat history is absent
(context isolation A1 ✓). Temperature forced to 0 (A1 determinism ✓).
Call count = 1 (≤2, A5 ✓).

## A2 — unsupported/weak claim (reject path)
```
[MODE] A2
[SIGNAL] CONTINUE
[REAL_READS] 3
[VERIFIER_CALLS] 1
[VERIFIER_CRITIQUE_EVENTS] 1
[TEMPERATURE_FORCED] [0]
[CONTEXT_LEAK_SUSPECTED] False
--- evidence_summary ---
- [file_system] content of core/_env.py: API_KEY handling here
- [file_system] content of llm_router.py: API_KEY handling here
- [file_system] content of core/config.py: API_KEY handling here
```
Verdict: FAIL → loop re-enters (CONTINUE), NO blind stamp. `verifier_critique`
event fired (1 event, goal_blocked=True). Context isolation + temperature=0 +
call count = 1 (A1 + A5 + critique event ✓).

## Wiring locations verified (post-4b5cc7d)
- `engine/_convergence.py:172` `_run_independent_checker` (over `evaluate_goal_exit`)
- `engine/_convergence.py:26` `_parse_verifier_verdict`, `:99` `_build_evidence_summary`
- `engine/_convergence.py:240` `verifier_critique` emit (subscribed `engine/kinetic.py:156`)
- `llm_router.py:281` `verifier_router`, `:284` `VERIFIER_SYSTEM_PROMPT`, `:309` `run_verifier_check`
- `engine/loop.py:162` `verifier_provider` DI, `:174` `_resolve_default_verifier`, `:175` `self._verifier_calls`

## KNOWN CONSTRAINT (out of scope, not fixed)
The Stage-6 verifier gate is LIVE only on the `main.py` CLI `ExecutionLoop` path.
`ui/repl_termux.py` calls `initialize_secure_agent()` which returns a smolagents
`CodeAgent` Manager — NOT an `ExecutionLoop` — so the REPL production path does
NOT exercise `verifier_router` at all. The separate `VerifierAgent` in
`core/multi_agent_orchestrator.py` is only reachable via `scripts/finalize.py`
and tests. See STATE.md "REPL verifier gap".
