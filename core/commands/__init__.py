"""core/commands — command handlers extracted from ui/ (V4 refactoring).

All modules here are UI-free: they accept plain Python objects and return
plain Python objects. The UI layer calls them and handles display.
"""
